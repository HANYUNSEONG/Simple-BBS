import { FlexCenter } from "@/theme/util";
import styled from "@emotion/styled";

export const NotFoundPostWrapper = styled.section`
  padding: 2rem 0;
  ${FlexCenter}
  flex-direction: column;
  row-gap: 1rem;

  h1 {
    font-weight: 100;
    letter-spacing: 2px;
    font-size: 2rem;
  }
`;
